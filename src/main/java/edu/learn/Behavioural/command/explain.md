## Command Pattern

Command wraps a request into an object.

### Example
- `LightOnCommand` and `LightOffCommand` encapsulate actions.
- `RemoteControl` executes commands without coupling to `Light`.
