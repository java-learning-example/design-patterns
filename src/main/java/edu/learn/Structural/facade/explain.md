## Facade Pattern

Facade provides a simplified interface to a complex subsystem.

### Example in code
- `HomeTheaterFacade.watchMovie()` wraps video decode, audio decode, and display.
- Client uses one method instead of orchestrating multiple objects.
