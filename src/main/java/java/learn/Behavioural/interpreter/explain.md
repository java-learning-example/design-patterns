## Interpreter Pattern

Interpreter defines grammar rules and evaluates expressions.

### Example
- `NumberExpression` is a terminal node.
- `AddExpression` combines expressions.
- Evaluates simple expression tree: `10 + 5`.
