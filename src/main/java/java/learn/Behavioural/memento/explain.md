## Memento Pattern

Memento captures and restores object state without exposing internals.

### Example
- `TextEditor` saves snapshots to `EditorMemento`.
- `History` keeps snapshots for undo behavior.
