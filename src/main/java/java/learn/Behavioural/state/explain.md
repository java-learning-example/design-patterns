## State Pattern

State lets an object change behavior when internal state changes.

### Example
- `MediaPlayer` delegates `pressPlay()` to current state.
- State transitions happen by swapping state objects.
