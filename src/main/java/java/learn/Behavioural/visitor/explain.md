## Visitor Pattern

Visitor puts operations in separate visitor classes instead of shape classes.

### Example
- Shapes accept visitors.
- `AreaCalculatorVisitor` adds behavior without changing shape classes.
