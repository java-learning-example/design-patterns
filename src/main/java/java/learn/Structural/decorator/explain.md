## Decorator Pattern

Decorator adds behavior dynamically without modifying original classes.

### Example in code
- Start with `SimpleCoffee`.
- Wrap it with `MilkDecorator` and `CaramelDecorator`.
- Final cost and description are composed at runtime.
